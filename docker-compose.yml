version: '3.8'

services:
  docs:
    build: .
    container_name: restoapp-docs
    ports:
      - "8000:8000"
    volumes:
      - .:/docs
    command: serve --dev-addr=0.0.0.0:8000
    restart: always
